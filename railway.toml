# Railway Configuration
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn app:app --timeout 120 --keep-alive 5 --max-requests 1000 --max-requests-jitter 50 --access-logfile - --error-logfile - --log-level info"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
